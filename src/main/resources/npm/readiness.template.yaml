- stage: readiness
  displayName: Readiness
  dependsOn: [ deploy ]
  condition: and(succeeded('deploy'), or(eq(variables.isDevelop, true), eq(variables.isRelease, true), eq(variables.isMaster, true)))
  variables:
    - group: DEV
  jobs:
    - job: readiness
      steps:
        - script: echo npm run readiness

