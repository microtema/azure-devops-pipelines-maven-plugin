- stage: build
  displayName: Build
  dependsOn: [ %DEPENDS_ON% ]
  condition: and( always(), %CONDITIONS% )
  variables:
    - group: DEV
  jobs:
    - job: build
      steps:
        - script: echo npm run build