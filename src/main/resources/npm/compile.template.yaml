- stage: compile
  displayName: Compile
  dependsOn: [ versioning ]
  condition: succeeded('versioning')
  variables:
    - group: DEV
  jobs:
    - job: compile
      steps:
        - script: echo npm install