- stage: versioning
  displayName: Versioning
  dependsOn: []
  condition: always()
  variables:
    - group: DEV
  jobs:
    - job: versioning
      steps:
        - script: echo npm run version -- $(VERSION)