- stage: deploy
  displayName: Deploy
  dependsOn: [ tag ]
  condition: and(succeeded('tag'), or(eq(variables.isDevelop, true), eq(variables.isRelease, true), eq(variables.isMaster, true)))
  variables:
    - group: DEV
  jobs:
    - job: deploy
      steps:
        - script: echo npm run deploy
