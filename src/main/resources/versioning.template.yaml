- stage: versioning
  displayName: Versioning
  dependsOn: []
  condition: always()
  variables:
    - group: ${{ variables.ENV_NAME }}
  jobs:
    - job: versioning
      steps:
        - script: echo npm run version -- $(VERSION)