- stage: tag
  displayName: Tag
  dependsOn: [ smoke_test ]
  condition: and(succeeded('readiness'), not(failed('smoke_test')), eq(variables.isMaster, true))
  variables:
    - group: ${{ variables.ENV_NAME }}
  jobs:
    - job: tag
      steps:
        - script: echo npm run tag
