- stage: tag
  displayName: Tag
  dependsOn: [ system_test ]
  condition: and(succeeded('system_test'), eq(variables.isMaster, true))
  variables:
    - group: ${{ variables.ENV_NAME }}
  jobs:
    - job: tag
      steps:
        - script: echo npm run tag
